/**
 * Firestore collection names
 */
export enum Collection {
  USERS = "users",
  COMMENTS = "comments",
  QUESTIONS = "questions",
  GRAPHS = "graphs",
}

export enum Difficulty {
  EASY = "EASY",
  MEDIUM = "MEDIUM",
  HARD = "HARD",
}
