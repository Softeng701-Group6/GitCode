import { Stack, Typography } from "@mui/material";

export default function LevelDescription() {

    return(
        <Stack>
            <Typography>
                THis is a heading
            </Typography>
        </Stack>
    )
}